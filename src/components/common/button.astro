---
import RightArrow from "@/assets/icons/rightarrow.svg";
import LogoStar from "@/assets/icons/staricon.svg";


const {
  type = "button",
  arrow = false,
  text,
  href,
  className,
  logo = false,
  onClick = undefined,
} = Astro.props;
---

{
  type === "button" || type === "submit" ? (
    <button
      type={type}
      class={`inline-flex items-center justify-center rounded-full cursor-pointer bg-button px-6 py-4 text-xl text-white 
        hover:bg-card transition-colors duration-300 ease-in-out
        ${className}`}
      onclick={onClick}
    >
      {logo && <LogoStar class="w-4 h-4 mx-1" />}
      {text}
      {arrow && <RightArrow class="w-4 h-4 mx-1" />}
    </button>
  ) : (
    <a
      class={`inline-flex items-center justify-center rounded-full cursor-pointer bg-button px-6 py-4 text-xl text-white 
        hover:bg-card transition-colors duration-300 ease-in-out
        ${className}`}
      href={href}
    >
      {logo && <LogoStar class="w-6 h-6 mx-1" />}
      {text}
      {arrow && <RightArrow class="w-4 h-4 mx-1" />}
    </a>
  )
}
