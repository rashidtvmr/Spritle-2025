<h1
  class="text-heading text-primary font-dmSans font-semibold lg:mt-25 md:mt-10 mt-5 text-center"
>
  Agile Development
</h1>
<div class="grid md:grid-cols-5 gap-[15px] container p-4">
  {
    [
      { id: "01", title: "01", description: " Discovery" },
      { id: "02", title: "02", description: " Discovery" },
      { id: "03", title: "03", description: " Development" },
      { id: "04", title: "04", description: " Deployment" },
      { id: "05", title: "05", description: " Continuous Optimization" },
    ].map((item, index) => (
      <div
        key={index}
        class="border-2 border-white bg-[#313131] rounded-[10px] p-[30px] max-w-[327px] max-h-[221px] flex flex-col justify-center items-center text-center lg:mt-20 md:mt-10 mt-5 relative overflow-hidden"
      >
        <div
          class="absolute inset-0 bg-primary opacity-0 animate-light-effect"
          style={{ animationDelay: `${index * 1.5}s` }}
        />
        <h2
          class="text-primary text-xl text-heading font-extrabold xl:pt-[20px] relative z-10 animate-text-color"
          style={{ animationDelay: `${index * 1.5}s` }}
        >
          {item.title}
        </h2>
        <p
          class="mt-2 text-primary font-semibold lg:text-[32px] text-lg xl:pt-[0px] relative z-10 animate-text-color"
          style={{ animationDelay: `${index * 1.5}s` }}
        >
          {item.description}
        </p>
      </div>
    ))
  }
</div>

<style>
  @keyframes light-effect {
    0%,
    100% {
      opacity: 0;
    }
    50% {
      opacity: 1;
    }
  }

  @keyframes text-color-change {
    0%,
    100% {
      color: inherit; /* Default text color */
    }
    50% {
      color: black; /* Text color when light is on */
    }
  }

  .animate-light-effect {
    animation: light-effect 7.5s infinite; /* Total duration for all cards */
  }

  .animate-text-color {
    animation: text-color-change 7.5s infinite; /* Sync with light effect */
  }
</style>
